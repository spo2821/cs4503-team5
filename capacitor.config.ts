import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.backseatboys.parkingBuddy',
  appName: 'ParkingBuddy',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
